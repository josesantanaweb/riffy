'use client';
import { useRouter } from 'next/navigation';

const Raffles = () => {
  const router = useRouter();

  return (
    <p className='text-white'>raffles</p>
  );
};

export default Raffles;
